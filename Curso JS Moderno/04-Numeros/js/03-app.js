let resultado;

// PI
resultado = Math.PI;

// Redondear
resultado = Math.round(2.8);
resultado = Math.round(2.2);
resultado = Math.round(2.6);
resultado = Math.round(2.5);
resultado = Math.round(2.4);

// Redondear hacia arriba
resultado = Math.ceil(2.1);

// Redondear hacia abajo
resultado = Math.floor(2.9);

// Raíz cuadrada 
resultado = Math.sqrt(144);

// Valor Absoluto (se trae el valor ignorando el signo)
resultado = Math.abs(-500);

// Potencia
resultado = Math.pow(8, 3/*el segundo numero es la potencia*/);

// Mínimo de una serie de valores
resultado = Math.min(3, 5, 1, 12, -3);

// Máximo de una serie de valores
resultado = Math.max(3, 5, 1, 12, -3);

// Aleatorio (tira un número random)
resultado = Math.random();

// Aleatorio dentro de un rango
resultado = Math.floor( Math.random() * 30)
console.log(resultado);