const producto = {
    nombre: "Monitor 20 Pulgadas",
    precio: 300,
    disponible: true
}

console.log(producto);

// Sintáxis de punto
console.log(producto.nombre);

// forma no muy común pero útil
console.log(producto['nombre'])